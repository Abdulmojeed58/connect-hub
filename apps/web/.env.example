# API base URL.
# Leave empty (or omit this file entirely) for local development — the Vite
# dev server proxies all /api requests to http://localhost:3001 by default.
# Set this for production or when the API runs on a different host/port.
VITE_API_BASE_URL=
